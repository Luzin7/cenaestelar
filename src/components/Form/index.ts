import FormButton from "./composition/FormButton";
import FormError from "./composition/FormError";
import FormWrapper from "./composition/FormWrapper";
import InputField from "./composition/InputField";

export const Form = {
  Wrapper: FormWrapper,
  Input: InputField,
  Button: FormButton,
  Error: FormError,
};
